const knexfile = require('../../../knexfile');
const knex = require('knex') (knexfile);

module.exports = knex;


/* importei o modulo do knexfile e configurei, 
neste momento já está pronto para ser importado*/